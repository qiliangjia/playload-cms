# A generated secret for Payload
PAYLOAD_SECRET=

# Cloudflare Pages deploy hooks（前端站点重建）
# 从 Cloudflare Pages 项目 → Settings → Builds & deployments → Deploy hooks 生成
CF_PAGES_DEPLOY_HOOK_TEST_URL=
CF_PAGES_DEPLOY_HOOK_PROD_URL=
